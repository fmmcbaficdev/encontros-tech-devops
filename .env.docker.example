# =============================================================================
# Encontros Tech — Template de Variáveis Docker Compose
# Copie para .env.docker e preencha com seus valores
# Este arquivo É seguro para commitar (sem credenciais reais)
# =============================================================================

# -----------------------------------------------------------------------------
# Aplicação
# -----------------------------------------------------------------------------
APP_NAME=encontros-tech-api
APP_VERSION=2.0.0
APP_ENV=development
PORT=8000
WORKERS=1
LOG_LEVEL=debug
CORS_ORIGINS=*

# -----------------------------------------------------------------------------
# PostgreSQL
# -----------------------------------------------------------------------------
POSTGRES_USER=encontros
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=encontros_tech
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# pgAdmin (opcional — profile: tools)
# -----------------------------------------------------------------------------
PGADMIN_DEFAULT_EMAIL=admin@encontros-tech.com
PGADMIN_DEFAULT_PASSWORD=CHANGE_ME
PGADMIN_PORT=5050
