apiVersion: v1
kind: ConfigMap
metadata:
  name: encontros-tech-config
  namespace: production
  labels:
    app: encontros-tech
    component: config
data:
  environment: "production"
  log_level: "info"
  workers: "2"
  app_name: "encontros-tech-api"
  version: "2.0.0"
  port: "8000"
  cors_origins: "https://encontros-tech.com"
